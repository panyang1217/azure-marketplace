{
  "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "vm": {
      "type": "object",
      "metadata": {
        "description": "Location where resources will be provisioned"
      }
    },
    "index": {
      "type": "int",
      "metadata": {
        "description": "the outer loop index"
      }
    },
    "diskEncryptionSettings": {
      "type": "secureObject",
      "metadata": {
          "description": "The encryption settings for Azure Disk Encryption"
      }
    },
    "elasticTags": {
      "type": "object",
      "defaultValue": {
        "provider": "648D2193-0CE0-4EFB-8A82-AF9792184FD9"
      },
      "metadata": {
        "description": "A unique identifier to allow the Azure Infrastructure to understand the origin of resources deployed to Azure. You do not need to supply a value for this."
      }
    }
  },
  "variables": {
    "namespace" : "[parameters('vm').namespace]",
    "computername": "[concat(variables('namespace'), parameters('index'))]"
  },
  "resources": [
    {
      "apiVersion": "2016-03-30",
      "type": "Microsoft.Compute/virtualMachines",
      "name": "[variables('computername')]",
      "location": "[parameters('vm').shared.location]",
      "tags": {
        "provider": "[toUpper(parameters('elasticTags').provider)]"
      },
      "properties": {
        "storageProfile": {
          "osDisk": {
            "encryptionSettings": "[parameters('diskEncryptionSettings').encryptionSettings]"
          }
        }
      }
    }
  ],
  "outputs": {
  }
}
